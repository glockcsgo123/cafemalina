# 🍕 Cafe Malina — TASKS v2
> Актуально: 02.06.2026 | cafemalina.vercel.app
> Стек: Next.js 15, TypeScript, Tailwind CSS 4, shadcn/ui, Zustand
> **Выполнять блоки строго по порядку. Каждый блок — отдельный коммит с понятным сообщением.**

---

## 🔴 БЛОК 1 — Критические баги (делать первым)

### 1.1 Защита админ-панели — /admin пускает без логина
**Проблема:** переход на `/admin` сразу открывает `/admin/menu` без запроса пароля.
**Что сделать:**
- Проверить `src/middleware.ts` — должен перехватывать все маршруты `/admin/*`
- Убедиться что middleware читает httpOnly cookie `admin_token` и делает redirect на `/admin/login` если cookie нет
- Проверить что `/admin/login` НЕ защищён middleware (иначе бесконечный redirect)
- Проверить API `/api/auth` — устанавливает ли cookie корректно после успешного входа
- Протестировать: открыть `/admin` в инкогнито → должно редиректить на `/admin/login`

```typescript
// src/middleware.ts — правильный вариант
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

export function middleware(request: NextRequest) {
  const isAdminRoute = request.nextUrl.pathname.startsWith('/admin')
  const isLoginPage = request.nextUrl.pathname === '/admin/login'
  const token = request.cookies.get('admin_token')

  if (isAdminRoute && !isLoginPage && !token) {
    return NextResponse.redirect(new URL('/admin/login', request.url))
  }
  return NextResponse.next()
}

export const config = {
  matcher: ['/admin/:path*']
}
```

### 1.2 Минимальный заказ 500 ₽
**Что сделать:**
- В компоненте корзины/чекаута найти кнопку «Оформить заказ»
- Добавить проверку: если `totalPrice < 500` — кнопка disabled
- Показывать под кнопкой текст: «Минимальный заказ — 500 ₽. Добавьте ещё на {500 - total} ₽»
- Текст исчезает когда сумма ≥ 500

```typescript
const isMinOrderMet = totalPrice >= 500
const remaining = 500 - totalPrice

// В JSX:
<Button disabled={!isMinOrderMet || !phoneComplete || !consentChecked}>
  Оформить заказ
</Button>
{!isMinOrderMet && (
  <p className="text-sm text-muted-foreground text-center mt-2">
    Минимальный заказ — 500 ₽. Добавьте ещё на {remaining} ₽
  </p>
)}
```

---

## 🟠 БЛОК 2 — Мобильная оптимизация

### 2.1 Шапка на мобильном — убрать текст номера телефона, оставить только иконку
**Файл:** `src/components/layout/Header.tsx`

**Проблема:** На мобильном в шапке показывается полный номер «+7 (910) 740-31-11» — занимает много места и выглядит криво.

**Что сделать:**
- На мобильном (`sm:hidden`) — показывать только иконку `Phone` без текста, как `<a href="tel:+79107403111">`
- На десктопе (`hidden sm:flex`) — показывать иконку + текст номера
- Убрать текст номера из мобильной версии полностью

```tsx
{/* Десктоп */}
<a href="tel:+79107403111" className="hidden sm:flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground transition-colors">
  <Phone className="h-4 w-4" />
  +7 (910) 740-31-11
</a>
{/* Мобильный — только иконка */}
<a href="tel:+79107403111" className="sm:hidden flex items-center justify-center w-9 h-9 rounded-full hover:bg-accent transition-colors">
  <Phone className="h-5 w-5" />
</a>
```

### 2.2 Карточки блюд на мобильном
**Файл:** компонент карточки меню

**Проблемы видны на скриншоте:**
- Знак «₽» отображается криво — скорее всего рендерится как отдельный элемент на новой строке
- Цена и кнопка «В корзину» не помещаются в одну строку

**Что сделать:**
- Цена: заменить `750 <br/> ₽` или `750\n₽` на единый элемент `<span>750 ₽</span>` — цена и знак рубля в одном теге
- Нижняя часть карточки: `flex items-center justify-between gap-2`
- Кнопка «В корзину»: добавить `whitespace-nowrap` чтобы текст не переносился
- На мобильном карточка должна быть 2 в ряд (`grid-cols-2`) — проверить что так и есть
- Минимальная ширина карточки: `min-w-0` чтобы flex не ломал layout

```tsx
{/* Цена — единым элементом */}
<span className="font-bold text-lg">{price} ₽</span>

{/* Нижняя часть */}
<div className="flex items-center justify-between gap-2 mt-auto pt-2">
  <span className="font-bold text-lg whitespace-nowrap">{price} ₽</span>
  <Button size="sm" className="whitespace-nowrap flex-shrink-0">
    + В корзину
  </Button>
</div>
```

### 2.3 Кнопка звонка — убрать странную тень
**Файл:** `src/components/CallButton.tsx`

**Проблема:** На скриншоте видна угловатая/прямоугольная тень вместо круглой.

**Что сделать:**
- Заменить `shadow-lg` на `shadow-[0_4px_20px_rgba(190,30,90,0.4)]` — цветная тень под цвет кнопки
- Убедиться что контейнер кнопки имеет `rounded-full overflow-hidden` — тень повторяет форму
- Добавить `hover:shadow-[0_6px_25px_rgba(190,30,90,0.5)] transition-shadow duration-200`

```tsx
<a
  href="tel:+79107403111"
  className="fixed bottom-8 right-6 z-50 flex items-center justify-center w-14 h-14 rounded-full bg-[#BE1E5A] text-white shadow-[0_4px_20px_rgba(190,30,90,0.4)] hover:shadow-[0_6px_25px_rgba(190,30,90,0.5)] hover:scale-110 transition-all duration-200"
>
  <Phone className="h-6 w-6" />
</a>
```

### 2.4 Общая мобильная оптимизация — аудит страниц
Пройтись по каждой странице на ширине 390px (iPhone 14) и исправить:

**Главная `/`:**
- Hero секция: текст не должен обрезаться, изображение пиццы — корректный размер
- Секция «Хиты» и «Рекомендуем»: карточки 2 в ряд или горизонтальный скролл
- Блок с условиями доставки: колонки → стек на мобильном

**Меню `/menu`:**
- Фильтр категорий: горизонтальный скролл, `overflow-x-auto scrollbar-hide`
- Карточки: `grid grid-cols-2 gap-3` на мобильном
- Поисковая строка: полная ширина

**Корзина / Чекаут:**
- Форма: поля на всю ширину
- Кнопка «Оформить»: `w-full`
- Блок upsell (добавить к заказу): горизонтальный скролл компактных карточек

---

## 🟡 БЛОК 3 — Корзина: улучшения UX

### 3.1 Фото в корзине
**Файл:** компонент корзины (sidebar или `/checkout`)

**Что сделать:**
- В списке позиций корзины добавить миниатюру фото слева от названия
- Размер: `w-16 h-16` (64×64px), `rounded-lg object-cover object-center`
- Если нет фото — заглушка с иконкой 🍽️ или серый прямоугольник

```tsx
<div className="flex items-center gap-3 py-2">
  <div className="relative w-16 h-16 rounded-lg overflow-hidden flex-shrink-0 bg-accent">
    <Image
      src={item.image || '/images/placeholder-roll.jpg'}
      alt={item.name}
      fill
      className="object-cover"
    />
  </div>
  <div className="flex-1 min-w-0">
    <p className="font-medium text-sm truncate">{item.name}</p>
    {item.selectedVariant && (
      <p className="text-xs text-muted-foreground">{item.selectedVariant}</p>
    )}
    <p className="text-sm font-semibold">{item.price} ₽</p>
  </div>
  {/* Счётчик количества */}
</div>
```

### 3.2 Время работы в корзине
**Что сделать:**
- Добавить блок с временем работы в корзину/чекаут — над кнопкой «Оформить заказ»
- Логика на клиенте:

```typescript
// Проверка рабочего времени
const now = new Date()
const hour = now.getHours()
const isOpen = hour >= 10 && hour < 23

// JSX:
{isOpen ? (
  <div className="flex items-center gap-2 text-sm text-green-600 bg-green-50 rounded-lg p-3">
    <Clock className="h-4 w-4 flex-shrink-0" />
    <span>Доставляем до 23:00 — принимаем заказы</span>
  </div>
) : (
  <div className="flex items-center gap-2 text-sm text-amber-600 bg-amber-50 rounded-lg p-3">
    <Clock className="h-4 w-4 flex-shrink-0" />
    <span>Сейчас не работаем. Доставка ежедневно с 10:00 до 23:00</span>
  </div>
)}
```

### 3.3 Порядок в блоке «Добавить к заказу»
**Что сделать:**
- Первым показывать соевый соус (`id: "soevyi-sous"`)
- Затем остальные напитки из категории `drinks`
- Проверить что соевый соус не дублируется (он и в соусах и вручную первым)

```typescript
// Правильная сортировка upsell-блока
const upsellItems = [
  menuItems.find(i => i.id === 'soevyi-sous'),          // 1. Соевый соус
  ...menuItems.filter(i => i.category === 'drinks'),     // 2. Напитки
].filter(Boolean)
```

### 3.4 Выбор способа оплаты
**Что сделать:**
- Добавить в форму чекаута блок «Способ оплаты» с тремя вариантами (radio-кнопки):
  - 💵 Наличными курьеру
  - 💳 Картой при получении
  - 📱 Переводом по номеру телефона
- По умолчанию выбрано «Наличными»
- Выбранный способ сохранять в данные заказа
- Красивый вид: карточки-переключатели с иконкой, названием

```tsx
const paymentMethods = [
  { id: 'cash', icon: '💵', label: 'Наличными курьеру' },
  { id: 'card', icon: '💳', label: 'Картой при получении' },
  { id: 'transfer', icon: '📱', label: 'Переводом по номеру телефона' },
]

// UI: горизонтальные карточки с radio
<div className="grid grid-cols-1 gap-2">
  {paymentMethods.map(method => (
    <label key={method.id} className={`flex items-center gap-3 p-3 rounded-xl border-2 cursor-pointer transition-colors ${
      selectedPayment === method.id
        ? 'border-[#BE1E5A] bg-[#BE1E5A]/5'
        : 'border-border hover:border-[#BE1E5A]/40'
    }`}>
      <input type="radio" name="payment" value={method.id} className="sr-only"
        onChange={() => setSelectedPayment(method.id)} />
      <span className="text-xl">{method.icon}</span>
      <span className="text-sm font-medium">{method.label}</span>
    </label>
  ))}
</div>
```

---

## 🟢 БЛОК 4 — Главная страница: правки

### 4.1 Убрать пункт «Готовим при заказе» из полоски преимуществ
**Файл:** `src/app/page.tsx` или компонент преимуществ на главной

**Что сделать:**
- Найти секцию с тремя пунктами: «Готовим при заказе» | «Доставка от 40 минут» | «Пицца, роллы, сеты и напитки»
- Удалить пункт «Готовим при заказе»
- Оставшиеся два пункта разместить по центру с разделителем между ними
- Или добавить другой третий пункт: «Оплата при получении» или «Ежедневно 10:00–23:00»

---

## 🔵 БЛОК 5 — Админ-панель: управление контентом

> Зависит от блока 1.1 (защита роутов должна работать)

### 5.1 Редактирование всех позиций меню
**Файл:** `src/app/admin/menu/page.tsx`

**Текущая проблема:** возможно не все позиции отображаются или нет удобного редактирования.

**Что должно быть:**
- Таблица/список ВСЕХ блюд из `data/menu.json` (или `menu.ts`)
- Для каждой позиции: миниатюра фото, название, категория, цена, кнопки «Редактировать» / «Удалить»
- Счётчик: «Всего блюд: 153»
- Фильтр по категории + поиск по названию

**Форма редактирования (модальное окно):**
```
Поля формы:
- Название (text input)
- Описание/состав (textarea)
- Категория (select со всеми категориями)
- Цена (number input)
- Вес (text input, напр. «210г» или «32/40 см»)
- Варианты (если есть) — можно добавить/удалить вариант с названием и ценой
- Фото — текущее фото + кнопка «Заменить фото» (file input → POST /api/upload)
- Флаг «Рекомендуем» (checkbox)
- Флаг «Хит заказов» (checkbox) — новое поле!
```

### 5.2 Управление «Хиты заказов» и «Рекомендуем» с главной
**Что сделать:**
- Добавить в `MenuItem` поле `isHit?: boolean` (аналогично `recommended`)
- В `/admin/menu` добавить быстрые переключатели прямо в таблице:
  ```
  [⭐ Рек.] [🔥 Хит] — кнопки-тоглы у каждой строки
  ```
- Отдельная вкладка в админке «Главная страница» где видно:
  - Текущие 4 хита (можно менять drag-and-drop или просто выбрать из списка)
  - Текущие 4 рекомендации
  - Кнопка «Сохранить порядок»

### 5.3 Редактирование главного экрана (Hero секция)
**Добавить в `/admin` раздел «Настройки сайта»:**
- Заголовок Hero («Пицца и роллы с доставкой»)
- Подзаголовок Hero
- Текст в полоске преимуществ (два пункта)
- Телефон кафе
- Адрес кафе
- Режим работы (открытие и закрытие)
- Минимальная сумма заказа (сейчас 500 ₽)
- Сумма для бесплатной доставки (сейчас 1000 ₽)

Хранить в `data/settings.json`:
```json
{
  "phone": "+79107403111",
  "address": "Курская обл., р.п. Прямицыно, ул. Коммунистическая, 28",
  "workingHours": { "open": 10, "close": 23 },
  "minOrderAmount": 500,
  "freeDeliveryAmount": 1000,
  "heroTitle": "Пицца и роллы с доставкой",
  "heroSubtitle": "Готовим каждый день: пицца, роллы, сеты — быстро и вкусно."
}
```

API:
```
GET  /api/settings          — публичный
PUT  /api/settings          — требует ADMIN_PASSWORD
```

---

## ⚫ БЛОК 6 — Финальный аудит и отчёт

> Этот блок выполняется ПОСЛЕДНИМ после всех остальных.
> Используй браузер (Claude in Chrome) для проверки каждого пункта.

### 6.1 Проверка всех исправлений

Пройдись по каждому пункту и напиши результат: ✅ / ❌ / ⚠️

**Безопасность:**
- [ ] `/admin` в инкогнито → редиректит на `/admin/login`
- [ ] `/admin/menu` в инкогнито → редиректит на `/admin/login`
- [ ] Неверный пароль → сообщение об ошибке, не пускает
- [ ] Верный пароль (`malina2024`) → пускает в `/admin/menu`

**Мобильная версия (390px):**
- [ ] Шапка: виден логотип, иконка телефона, иконка корзины — всё в одну строку
- [ ] Номер телефона в шапке НЕ показывается текстом на мобильном
- [ ] Карточки блюд: знак ₽ и цена в одной строке, не переносится
- [ ] Кнопка «В корзину» целиком видна, не обрезается
- [ ] Кнопка звонка: круглая тень (не прямоугольная)

**Корзина:**
- [ ] Фото товаров отображается в списке позиций корзины
- [ ] Блок «Добавить к заказу»: первый — Соевый соус, затем напитки
- [ ] Отображается рабочее/нерабочее время
- [ ] Блок «Способ оплаты» с тремя вариантами
- [ ] Минимальный заказ 500 ₽: при сумме ниже кнопка заблокирована
- [ ] Текст «Добавьте ещё на X ₽» при сумме ниже 500

**Главная:**
- [ ] Пункт «Готовим при заказе» ОТСУТСТВУЕТ в полоске преимуществ

**Админка:**
- [ ] Все 153 позиции видны в `/admin/menu`
- [ ] Можно отредактировать название блюда → изменения применяются на сайте
- [ ] Можно заменить фото блюда
- [ ] Можно отметить блюдо как «Хит» или «Рекомендуем»
- [ ] Изменение «Хита» → на главной показывается обновлённый список

### 6.2 Проверка консоли браузера
- [ ] `/` — нет красных ошибок в DevTools Console
- [ ] `/menu` — нет красных ошибок
- [ ] `/checkout` — нет красных ошибок
- [ ] `/admin/menu` — нет красных ошибок
- [ ] Нет 404 в Network tab для изображений

### 6.3 Финальный отчёт
После проверки всех пунктов сформируй отчёт в следующем формате:

```
## Отчёт о выполнении задач — [дата]

### ✅ Выполнено успешно (N пунктов)
- список...

### ⚠️ Выполнено с замечаниями (N пунктов)
- пункт: описание замечания + скриншот

### ❌ Не выполнено / сломано (N пунктов)
- пункт: описание проблемы + что нужно сделать

### 📊 Итог
- Общий балл: X/Y
- Критических багов: N
- Рекомендации: ...
```

---

## 📋 Сводка по порядку выполнения

| № | Блок | Задачи | Сложность | Зависимости |
|---|------|--------|-----------|-------------|
| 1 | 🔴 Критические баги | Защита /admin, минимальный заказ | Низкая | — |
| 2 | 🟠 Мобилка | Шапка, карточки, тень кнопки, аудит | Средняя | — |
| 3 | 🟡 Корзина UX | Фото, время, порядок upsell, оплата | Средняя | — |
| 4 | 🟢 Главная | Убрать «Готовим при заказе» | Низкая | — |
| 5 | 🔵 Админка | Контент, хиты, настройки | Высокая | Блок 1 |
| 6 | ⚫ Аудит | Проверка всего + отчёт | — | Все блоки |

---

## 🔑 Справочная информация

**.env.local:**
```
ADMIN_PASSWORD=malina2024
```

**Телефон:** +7 (910) 740-31-11 → `tel:+79107403111`
**Адрес:** Курская обл., р.п. Прямицыно, ул. Коммунистическая, 28
**Режим работы:** 10:00–23:00 ежедневно
**Мин. заказ:** 500 ₽
**Бесплатная доставка:** от 1000 ₽

**npm install (если ещё не установлено):**
```bash
npm install react-input-mask
npm install --save-dev @types/react-input-mask
```
