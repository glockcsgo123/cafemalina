# 🧪 Cafe Malina — Тест + Дизайн-правки
> Актуально: 02.06.2026 | cafemalina.vercel.app

---

## ЧАСТЬ 1 — ДИЗАЙН: что улучшить

### 🔴 Проблемы которые бросаются в глаза

**1. Карточки блюд — несогласованная высота фото**
Фото пиццы сделаны в разных условиях: одни светлые, другие тёмные, разные пропорции.
Карточки смотрятся неровно. Исправить в `MenuCard`:
```css
/* Сейчас что-то вроде */
<img className="w-full h-48 object-cover">
/* Добавить строгий aspect-ratio */
<div className="aspect-[4/3] overflow-hidden rounded-t-xl">
  <img className="w-full h-full object-cover object-center">
```

**2. Кнопка звонка перекрывает контент**
Плавающая кнопка `fixed bottom-6 right-6` на некоторых экранах перекрывает кнопку «В корзину» крайней правой карточки. Сдвинуть на `bottom-8 right-8` и добавить тень.

**3. Поисковая строка выглядит «плоско»**
Сейчас просто outlined input. Добавить лёгкую тень и увеличить высоту:
```css
className="h-12 shadow-sm border-gray-200 focus:border-[#BE1E5A] focus:ring-[#BE1E5A]/20"
```

**4. Фильтр категорий — активная кнопка слишком похожа на остальные**
Увеличить контраст активной вкладки: добавить небольшую тень `shadow-md` к активной кнопке.

**5. Секция «Рекомендуем» — бейдж «⭐ Рекомендуем» задвоен с бейджем цены**
На карточках одновременно два бейджа в углах — визуально перегружено.
Решение: убрать звезду из бейджа слева, оставить только текст «Рекомендуем» маленьким шрифтом.

**6. Главная — слишком большой gap между секциями**
Между «Хиты заказов» и «Рекомендуем» и блоком доставки — огромные пустые отступы.
Уменьшить `py-20` → `py-12` в секциях главной страницы.

**7. Footer — ссылка «Политика конфиденциальности» отсутствует визуально**
Нужно убедиться что она добавлена под копирайтом (из скриншота не было видно).

### 🟡 Улучшения которые сделают сайт лучше

**8. Hover-эффект на карточках блюд**
Добавить плавный zoom на фото при наведении:
```css
<img className="... transition-transform duration-300 group-hover:scale-105">
<div className="... overflow-hidden group">
```

**9. Skeleton-загрузка для карточек**
Если меню грузится с API — добавить skeleton-плейсхолдеры вместо пустого экрана.

**10. Кнопки размеров пиццы (32/40 см) — не очевидно что можно выбрать**
Добавить cursor-pointer и hover-эффект на неактивный размер.

---

## ЧАСТЬ 2 — ПРОМПТ ДЛЯ ТЕСТИРОВАНИЯ

Скопируй целиком и отправь в Claude Code:

---

```
Протестируй сайт cafemalina.vercel.app и проверь каждый пункт из списка ниже.
Используй браузер (Claude in Chrome). По каждому пункту напиши: ✅ работает / ❌ не работает / ⚠️ работает с замечанием.

## ТЕСТ 1 — Главная страница (/)

1.1 Открой / — загружается без ошибок в консоли
1.2 Появляется cookie-баннер внизу экрана
1.3 Нажми «Принять» — баннер скрывается и больше не появляется при перезагрузке
1.4 В хедере виден телефон +7 (910) 740-31-11 с иконкой
1.5 Клик на телефон в хедере — открывает tel: ссылку
1.6 Видна плавающая кнопка звонка (малиновый круг) в правом нижнем углу
1.7 Секция «Хиты заказов» — все 4 карточки с фото (не серые)
1.8 Секция «Рекомендуем попробовать» — видна ниже хитов, 4 карточки с фото
1.9 Блок с условиями доставки присутствует на странице
1.10 Кнопка «Перейти в меню» ведёт на /menu

## ТЕСТ 2 — Страница меню (/menu)

2.1 Все категории в фильтре: Все, Пицца, Роллы, Запечённые и горячие, Премиум, Сеты, Пивные сеты, Напитки, Соусы
2.2 Категории «Фаст-фуд», «Горячие роллы», «Сливочные роллы» — ОТСУТСТВУЮТ в фильтре
2.3 Кликни «Пицца» — показываются только пиццы, страница плавно скроллит к секции
2.4 Кликни «Роллы» — показываются роллы (включая бывшие сливочные)
2.5 Кликни «Сеты» — НЕТ блюд «Статус» и «Самурай» (они перенесены в роллы)
2.6 У всех карточек есть фото (проверь 10-15 штук из разных категорий)
2.7 Строка поиска работает: введи «Филадельфия» — показываются только филадельфии
2.8 Поиск «фывфыв» — показывается сообщение «Ничего не найдено»
2.9 Кнопка × очищает поиск
2.10 Клик «В корзину» — счётчик в хедере увеличивается
2.11 Клик на фото или название блюда — открывается модальное окно (если реализовано)
2.12 В модальном окне виден полный состав, фото, кнопка «В корзину»
2.13 Клик на фото в модалке — открывается лайтбокс на весь экран
2.14 Закрытие модалки по клавише Escape
2.15 Фильтр «прилипает» к шапке при скролле страницы

## ТЕСТ 3 — Корзина и оформление заказа

3.1 Добавь 2-3 разных блюда в корзину
3.2 Открой корзину — все блюда видны с ценами и количеством
3.3 Перейди к оформлению заказа
3.4 Поле телефона имеет маску: при вводе автоматически форматируется как +7 (___) ___-__-__
3.5 Нельзя ввести буквы в поле телефона
3.6 Кнопка «Оформить» заблокирована при незаполненном телефоне
3.7 Есть степпер «Количество персон» (кнопки − / +)
3.8 Степпер не позволяет выставить меньше 1 персоны
3.9 Виден блок «Добавить к заказу?» с соевым соусом и напитками
3.10 Можно добавить напиток прямо из этого блока
3.11 Есть чекбокс согласия с обработкой персональных данных
3.12 Ссылка в чекбоксе ведёт на /privacy (открывается в новой вкладке)
3.13 Кнопка «Оформить» заблокирована без галочки согласия
3.14 На странице /checkout НЕТ плавающей кнопки звонка (не мешает форме)
3.15 Заполни форму полностью и отправь заказ — появляется экран успеха

## ТЕСТ 4 — Страницы сайта

4.1 /delivery — открывается без 404, есть условия доставки и зона
4.2 /about — открывается без 404, есть адрес и телефон
4.3 /privacy — открывается без 404, есть текст политики с упоминанием ФЗ-152
4.4 Ссылка «Политика конфиденциальности» есть в футере

## ТЕСТ 5 — Футер

5.1 В футере «Наборы» заменено на «Сеты»
5.2 В футере нет ссылки «Фаст-фуд»
5.3 Телефон в футере кликабелен (tel: ссылка)
5.4 Есть ссылка «Политика конфиденциальности»

## ТЕСТ 6 — Мобильная версия (эмуляция 390px)

Открой DevTools → мобильная эмуляция iPhone 14 (390×844):

6.1 Хедер: телефон скрыт или показана только иконка (не ломает layout)
6.2 Меню гамбургер работает (если есть)
6.3 Фильтр категорий горизонтально скроллится без видимой полосы
6.4 Карточки блюд — 2 в ряд или 1 в ряд, не обрезаются
6.5 Кнопка «В корзину» полностью видна на каждой карточке
6.6 Плавающая кнопка звонка не перекрывает кнопки добавления
6.7 Форма заказа удобно заполняется на мобильном
6.8 Маска телефона корректно работает на мобильной клавиатуре

## ТЕСТ 7 — Консоль браузера (критично)

Открой DevTools → Console:
7.1 На / нет ошибок красного цвета
7.2 На /menu нет ошибок красного цвета
7.3 На /checkout нет ошибок красного цвета
7.4 Нет ошибок 404 для изображений в Network tab
7.5 npm run build проходит без TypeScript ошибок

---

По каждому тесту напиши результат, и для ❌ / ⚠️ — скриншот и краткое описание что именно не так.
```

---

## ЧАСТЬ 3 — Дизайн-промпт для Claude Code

```
Улучши дизайн сайта Cafe Malina. Не меняй структуру и логику — только визуальные детали.
Все изменения должны сохранять существующую малиновую цветовую схему (#BE1E5A).

### Изменения:

1. **Карточки блюд** (`src/components/menu/MenuCard.tsx` или аналогичный):
   - Обернуть блок с фото в `<div className="overflow-hidden rounded-t-xl">` если ещё не так
   - Добавить к `<img>` классы `transition-transform duration-300 group-hover:scale-105`
   - Добавить к карточке (корневой div) класс `group`
   - Убедиться что фото имеет строгое соотношение: `aspect-[4/3] w-full object-cover object-center`

2. **Поисковая строка на /menu**:
   - Высота `h-12` (48px)
   - Добавить `shadow-sm`
   - При фокусе: `ring-2 ring-[#BE1E5A]/20 border-[#BE1E5A]`
   - Увеличить border-radius до `rounded-xl`

3. **Фильтр категорий**:
   - Активная кнопка: добавить `shadow-md` и убрать border (оставить только bg)
   - Неактивная кнопка при hover: `hover:bg-gray-100 transition-colors`

4. **Отступы между секциями главной страницы**:
   - Найти секции с `py-20` или `py-24` → уменьшить до `py-12` или `py-14`
   - Между «Хиты заказов» и «Рекомендуем» должно быть единообразное расстояние

5. **Плавающая кнопка звонка** (`src/components/CallButton.tsx`):
   - Изменить позицию: `bottom-8 right-6` (чуть выше от края)
   - Добавить `shadow-lg hover:shadow-xl transition-shadow`
   - Добавить `hover:scale-110 transition-transform`

6. **Бейдж «Рекомендуем» на карточках**:
   - Уменьшить размер: `text-xs px-2 py-0.5`
   - Убрать звезду ⭐ (или оставить, но уменьшить)
   - Сделать полупрозрачный фон: `bg-[#BE1E5A]/90 backdrop-blur-sm`

7. **Hover на кнопках размеров пиццы (32/40 см)**:
   - Неактивный вариант: добавить `hover:bg-gray-100 cursor-pointer transition-colors`

8. **Анимация появления карточек при скролле** (опционально, если легко реализовать):
   - Добавить Tailwind-класс `animate-fadeIn` или использовать `Intersection Observer`
   - Карточки появляются с лёгким `opacity-0 → opacity-100 translate-y-4 → translate-y-0`

После каждого изменения запусти `npm run dev` и проверь что ничего не сломалось.
Сделай `npm run build` в конце.
```
